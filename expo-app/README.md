MedCalc Expo app

Quick start

1. Install dependencies:

```bash
cd expo-app
npm install
```

2. Provide Firebase web config: edit `./firebase/firebaseConfig.js` and set `firebaseWebConfig` with your project's web credentials (apiKey, authDomain, etc.).

3. Start development:

```bash
npm run start
```